"""Sheet-by-sheet port of the acquisition/rehab underwriting workbook.

Each module corresponds to one tab of `reference/Acq_Rehab_Model_v1.xlsx` and
carries that tab's cell references in comments, so any number the engine
produces can be traced back to the spreadsheet it came from.
"""
